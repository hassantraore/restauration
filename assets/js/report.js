/* function changeYear(year) {
    $.ajax({
        url: "/admin/dashboard/" + year,
        method: "POST",
        headers: {
            "X-Requested-With": "XMLHttpRequest",
        },
    }).then((response) => {
        if (document.querySelector(".dashboard")) {
            document.querySelector(".dashboard").innerHTML = data.item;
        }
    });
} */
