{% extends 'base.html.twig' %} {% block title %}Plat{% endblock %} {% block body
%}
<h1>Plat</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ product.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ product.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ product.description }}</td>
        </tr>
        <tr>
            <th>Size</th>
            <td>{{ product.size ? product.size|join(', ') : '' }}</td>
        </tr>
        <tr>
            <th>Price</th>
            <td>{{ product.price }}</td>
        </tr>
        <tr>
            <th>Promotion</th>
            <td>{{ product.promotion ? "Yes" : "No" }}</td>
        </tr>
        <tr>
            <th>ImageName</th>
            <td>{{ product.imageName }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('app_admin_products') }}">back to list</a>

<a href="{{ path('app_product_edit', { id: product.id }) }}">edit</a>

{{ include("admin/plats/_delete_form.html.twig") }}
{% endblock %}
