{% extends 'base.html.twig' %} {% block title %}Drink{% endblock %} {% block
body %}
<h1>Drink</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ drink.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ drink.name }}</td>
        </tr>
        <tr>
            <th>Price</th>
            <td>{{ drink.price }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('app_admin_drinks') }}">back to list</a>

<a href="{{ path('app_drink_edit', { id: drink.id }) }}">edit</a>

{{ include("/admin/drinks/actions/_delete_form.html.twig") }}
{% endblock %}
