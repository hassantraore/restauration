{% extends 'base.html.twig' %} {% block title %}Extras{% endblock %} {% block
body %}
<h1>Extras</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ extra.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ extra.name }}</td>
        </tr>
        <tr>
            <th>Price</th>
            <td>{{ extra.price }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('app_admin_extras') }}">back to list</a>

<a href="{{ path('app_extras_edit', { id: extra.id }) }}">edit</a>

{{ include("extras/_delete_form.html.twig") }}
{% endblock %}
